import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // RecoverFlow client pages intentionally fetch external API data on mount and
      // update local state when those async requests resolve. React's compiler-style
      // heuristic flags helper calls from effects even though the updates happen after
      // network awaits. Keep the correctness/dependency rules enabled, but do not treat
      // this mount-loading pattern as a lint error.
      "react-hooks/set-state-in-effect": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
